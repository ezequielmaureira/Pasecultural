 Informe Técnico — Proyecto " pasecultural " Ubicación: c:\Users\ezequ\Desktop\pasecultural Repo git : sí ( tiene . git ) Estructura general: monorepo simple con dos carpetas independientes backend / y frontend /, cada una con su propio package.json y node_modules (no es un workspace npm formal; hay un package.json suelto en la raíz, ver sección 5). 1. BACKEND Ubicación: c:\Users\ezequ\Desktop\pasecultural\backend Stack: Node.js (ESM, " type ": "module") + Express 5 + Prisma ORM 6 + PostgreSQL + Clerk ( auth ) + bcrypt / jsonwebtoken (dependencias instaladas pero no usadas actualmente en el código visto). 1.1 backend / package.json main : src /server.js Scripts: dev → nodemon src /server.js, start → node src /server.js Dependencias: @clerk/express ^2.1.40 — middleware de autenticación de Clerk para Express @prisma/client ^6.19.3 bcrypt ^6.0.0 (no se usa en el código actual) cors ^2.8.6 dotenv ^17.4.2 express ^5.2.1 jsonwebtoken ^9.0.3 (no se usa en el código actual) pg ^8.22.0 (driver Postgres , usado indirectamente por Prisma) prisma ^6.19.3 devDependencies : nodemon ^3.1.14 1.2 Entry point y configuración de la app backend / src /server.js : carga dotenv / config , importa app.js, escucha en process.env.PORT || 3000. backend / src /app.js : configura Express con cors ( ), express.json (), y clerkMiddleware ( ) de @clerk/express. Expone: GET /api/ health → healthcheck simple ({ ok : true, message : " PaseCultural API funcionando 🚀 " } ) Monta routers : /api/ users , /api/ auth , /api/ organizations backend / src / config /prisma.js : instancia única de PrismaClient exportada por defecto ( singleton simple, sin manejo especial de conexión). 1.3 Estructura de backend / src / src / ├ ── app.js ├ ── server.js ├ ── config / │ └── prisma.js ├ ── controllers / │ ├ ── auth.controller.js │ ├ ── organization.controller.js │ └── user.controller.js ├ ── routes / │ ├ ── auth.routes.js │ ├ ── organization.routes.js │ └── user.routes.js └── services / ├ ── auth.service.js └── organization.service.js Patrón: Router → Controller (maneja req /res, status codes ) → Service (lógica de negocio y acceso a Prisma). No hay carpeta middlewares/ propia (solo se usa el middleware de Clerk directamente en app.js). No hay capa de " models " separada — el modelo de datos vive en el schema de Prisma. 1.4 Endpoints (API REST) Método Ruta Controller Descripción GET /api/ health ( inline en app.js) Healthcheck POST /api/ auth / sync syncUser (auth.controller.js) Sincroniza el usuario autenticado de Clerk con la tabla User local. Requiere token de Clerk (usa getAuth ( req )); si no hay userId retorna 401. Obtiene datos del usuario vía clerkClient.users.getUser ( userId ) y llama a syncUserService . GET /api/ users getUsers (user.controller.js) Lista todos los usuarios ( prisma. user.findMany ()), sin paginación ni filtro por rol — no está protegido explícitamente por rol en el controller (solo pasa por clerkMiddleware ( ) global, que no bloquea rutas sin requireAuth ( )). GET /api/ users / count getUsersCount Retorna { total } con prisma. user.count (). GET /api/ organizations /me getMyOrganization Retorna la organización del usuario autenticado (busca por ownerId derivado de clerkId ). 401 si no autenticado. POST /api/ organizations createOrganization Crea una organización para el usuario autenticado. Requiere name y email en el body (400 si faltan). Si el usuario no fue sincronizado antes (USER_NOT_SYNCED), retorna 409. Si ya existe organización del usuario, la devuelve sin duplicar. Si el usuario tenía rol CUSTOMER, lo asciende a ORGANIZER tras crear la organización. Nota de seguridad : ninguna ruta usa explícitamente requireAuth ( ) de Clerk como middleware de router ; la protección se hace manualmente dentro de cada controller chequeando getAuth ( req ). userId . La ruta GET /api/ users no verifica autenticación en absoluto — cualquiera puede listar todos los usuarios (posible issue a señalar). 1.5 Lógica de negocio ( services ) auth.service.js : syncUserService ( { clerkId , email, firstName , lastName , imageUrl }) — busca usuario por clerkId ; si no existe, lo crea. Asigna rol DEVELOPER si el email está en un array hardcodeado DEVELOPERS = ["ezequiel.maureira@gmail.com"] (coincide con el email del usuario actual), sino CUSTOMER por defecto. Devuelve un DTO limitado del usuario (sin exponer campos internos extra). organization.service.js : getUserByClerkId, getMyOrganizationService(clerkId), createOrganizationService( clerkId, data) — crea organización con status PENDING, evita duplicados (un usuario = una organización, buscando por ownerId ), y promueve automáticamente el rol del usuario de CUSTOMER a ORGANIZER. 1.6 Variables de entorno backend ( backend /. env , no versionado, contiene secretos reales) DATABASE_URL="postgresql://postgres:***@localhost:5432/pasecultural" CLERK_PUBLISHABLE_KEY=" pk_test _..." CLERK_SECRET_KEY=" sk_test _..." ⚠️ Este . env tiene credenciales reales (contraseña de Postgres local y claves de Clerk de test). Está correctamente listado en . gitignore , pero conviene recordar rotar si alguna vez se filtró. No hay . env .example en el backend . backend /. gitignore : ignora node_modules , . env , / generated /prisma. 1.7 backend / prisma.config.ts Config nueva estilo Prisma 6 (reemplaza flags CLI): define schema : "prisma/ schema.prisma ", migrations.path : "prisma/ migrations ", y toma datasource.url desde process.env.DATABASE _URL (carga dotenv / config ). 1.8 Carpeta backend / generated / Existe (probablemente el output de prisma generate con el output custom , dado que está en . gitignore como / generated /prisma). No se inspeccionó en detalle (es código autogenerado). 2. FRONTEND Ubicación: c:\Users\ezequ\Desktop\pasecultural\frontend Stack: React 19 + Vite 8 + React Router DOM 7 + Tailwind CSS 4 (vía plugin @tailwindcss/vite) + Clerk React (@clerk/clerk-react) para auth + lucide- react para iconos. 2.1 frontend / package.json Scripts: dev (vite), build (vite build ), preview (vite preview ) Dependencias: @clerk/clerk-react ^5.61.9 @clerk/localizations ^4.13.2 (usa esES — localización español ) lucide- react ^1.24.0 react / react -dom ^19.2.7 react - router -dom ^7.18.1 devDependencies : @tailwindcss/vite ^4.3.2, tailwindcss ^4.3.2 @vitejs/plugin-react ^6.0.3 vite ^8.1.4 No hay librería de manejo de estado global ( Redux / Zustand ) — se usa React Context ( AuthContext , OrganizerDataContext ) con datos mock locales. No hay librería de formularios ( react-hook-form , etc.) — formularios manejados con useState manual. 2.2 frontend /vite.config.js Config mínima: plugins react ( ) y tailwindcss ( ). 2.3 Entry point frontend /index.html : HTML base, monta #root. frontend / src / main.jsx : crea el root de React , envuelve la app en &lt; ClerkProvider &gt; (usando VITE_CLERK_PUBLISHABLE_KEY desde env , lanza error si falta), con localización esES, signInUrl="/iniciar-sesion", signUpUrl="/registro". frontend / src / App.jsx : define el ruteo completo con react - router -dom v7 ( BrowserRouter , Routes /Route, Outlet). Envuelve todo en AuthProvider . 2.4 Rutas / páginas ( React Router ) Públicas (dentro de PublicShell ): / → Home.jsx / para-organizadores → OrganizersLanding.jsx /iniciar- sesion → SignIn.jsx (probablemente wrapper de &lt; SignIn &gt; de Clerk) /registro → SignUp.jsx ( wrapper de &lt; SignUp &gt; de Clerk) Protegidas (requieren RequireAuth , que usa &lt; SignedIn &gt;/&lt; SignedOut &gt;/&lt; RedirectToSignIn &gt; de Clerk): /bienvenida → PostAuth.jsx — pantalla de "preparando cuenta" que redirige según rol (ROLE_HOME) una vez que AuthContext termina de sincronizar con el backend . /organizador/nueva- organizacion → OrganizerOnboarding.jsx — formulario de alta de organización (llama a POST /api/ organizations ). Dentro de AppShell (con Sidebar + Topbar ): / developer → DashboardDeveloper.jsx /organizador (con OrganizerDataProvider envolviendo sub-rutas vía mock data): index → OrganizerDashboard.jsx /organizador/eventos → OrganizerEvents.jsx /organizador/eventos/nuevo → OrganizerEventForm.jsx /organizador/eventos/:id/editar → OrganizerEventForm.jsx /organizador/entradas → OrganizerTickets.jsx /organizador/ventas → OrganizerSales.jsx /organizador/ scanners → OrganizerScanners.jsx /organizador/ configuracion → OrganizerSettings.jsx /scanner → DashboardScanner.jsx /usuario → DashboardCustomer.jsx * (dentro de AppShell ) → NotFound (componente inline en App.jsx ) 2.5 Sistema de roles ( frontend / src / lib /roles.js) 4 roles: developer , organizer , scanner, customer — coinciden con el enum Role de Prisma (DEVELOPER, ORGANIZER, SCANNER, CUSTOMER). ROLE_HOME mapea rol → ruta home. roleFromPath ( pathname ) infiere el rol activo desde la URL actual (para que Sidebar muestre el menú correcto) — nota : esto es solo cosmético/basado en URL, no en el rol real del usuario autenticado; el Sidebar no restringe navegación por rol real. 2.6 Contextos AuthContext.jsx ( AuthProvider , hook useBackendUser ): al detectar sesión de Clerk ( useAuth , useUser ), llama a POST /api/ auth / sync con el token de Clerk y guarda el usuario " backend " ( backendUser ) sincronizado (con su rol) en estado. Expone { backendUser , syncing } . OrganizerDataContext.jsx ( OrganizerDataProvider , hook useOrganizerData ): estado completamente mock /local (no persiste a backend ) para eventos, ventas, scanners , escaneos recientes. Provee CRUD in-memory: addEvent, updateEvent, removeEvent, duplicateEvent, addScanner, updateScanner, removeScanner. Usa datos iniciales de data/organizerMockData.js (304 líneas — incluye EVENT_STATUS, INITIAL_EVENTS, INITIAL_SALES, INITIAL_SCANNERS, RECENT_SCANS). 2.7 Capa de API ( frontend / src / lib /api.js) Función apiFetch ( path , {token, ... options }) — wrapper simple sobre fetch , usa VITE_API_URL (default http://localhost:3000), agrega header Authorization : Bearer &lt;token&gt; si se pasa token, lanza error si res.ok es falso. Es la única función de comunicación con el backend ; usada en AuthContext y OrganizerOnboarding . 2.8 Componentes components / auth / RequireAuth.jsx : guard de rutas basado en Clerk ( SignedIn / SignedOut ). components / layout / : AppShell.jsx — layout con Sidebar fija + Topbar + &lt;Outlet&gt; (para vistas internas por rol). PublicShell.jsx — header público con nav , botones de búsqueda/carrito (sin funcionalidad, solo UI), login / signup con Clerk ( SignedIn / SignedOut , UserButton ), y Footer. Sidebar.jsx — sidebar con navegación dependiente del rol (NAV_BY_ROLE), infiere rol vía roleFromPath . Muchos ítems de nav están deshabilitados ( DisabledNavItem , sin path ) — funcionalidad pendiente de implementar ( ej : "Eventos", "Entradas", "Usuarios" del developer ). Footer.jsx , Topbar.jsx . components / organizers / DashboardPreview.jsx — componente visual usado probablemente en el landing de organizadores. components/ui/ : Avatar.jsx, Button.jsx, Card.jsx, FormField.jsx (exporta Field, inputClass, textareaClass), Modal.jsx, SearchInput.jsx — kit de UI propio con Tailwind (no usa una librería de componentes de terceros). 2.9 Data mock ( frontend / src /data/) organizerMockData.js (304 líneas) — datos de eventos/ventas/ scanners para el panel de organizador (todo hardcodeado , sin conexión real a la API todavía salvo auth /organización). organizersLandingData.js (104 líneas) — contenido para landing de organizadores. publicMockData.js (105 líneas) — datos mock para la home pública (eventos destacados, etc. presumiblemente). 2.10 Páginas de dashboard por rol DashboardDeveloper.jsx (235 líneas) — panel admin / dev , probablemente con métricas globales (usa /api/ users / count posiblemente). DashboardScanner.jsx (87 líneas) — panel para rol scanner (QR). DashboardCustomer.jsx (100 líneas) — panel para usuarios finales. Páginas de organizador ( pages / organizer /): Dashboard , Events (lista), EventForm (crear/editar, 376 líneas — el más grande), Tickets, Sales, Scanners , Settings — todo consumiendo datos mock del OrganizerDataContext , no hay endpoints backend para eventos/tickets/ventas/ scanners todavía (solo existen modelos User y Organization en Prisma). 2.11 Variables de entorno frontend ( frontend /. env , no versionado) VITE_CLERK_PUBLISHABLE_KEY= pk_test _... No hay VITE_API_URL seteado (usa el default http://localhost:3000 de api.js). No existe . env .example en frontend tampoco. frontend /. gitignore : ignora node_modules , dist , dist-ssr , *.local, . env . 2.12 Estilos frontend / src / styles /index.css — probablemente el entrypoint de Tailwind v4 (@import " tailwindcss " style ). Paleta oscura consistente en toda la app (#05070B, #0B1120, acentos violeta /azul). 2.13 Carpeta frontend / dist / Presente ( build generado, debería estar gitignored — confirmar que está incluida en . gitignore , sí lo está). 3. BASE DE DATOS (Prisma / PostgreSQL) Schema: backend /prisma/ schema.prisma Provider : PostgreSQL ( env ("DATABASE_URL")) Generator : prisma- client - js Enums Role: DEVELOPER | ORGANIZER | SCANNER | CUSTOMER OrganizationStatus : PENDING | APPROVED | REJECTED | SUSPENDED Modelo User Campo Tipo Notas id String ( cuid ) PK clerkId String único, vínculo con Clerk email String único firstName String? lastName String? imageUrl String? role Role default CUSTOMER createdAt DateTime default now ( ) updatedAt DateTime @updatedAt organizations Organization [ ] relación 1-a-muchos (un usuario puede ser owner de varias orgs a nivel de schema , aunque la lógica de negocio actual limita a 1) Modelo Organization Campo Tipo Notas id String ( cuid ) PK name String description String? logo String? phone String? email String website String? cuit String? status OrganizationStatus default PENDING createdAt / updatedAt DateTime approvedAt DateTime ? approvedBy String? (guarda algún identificador, no es relación FK) ownerId String FK → User.id owner User relación Migraciones ( backend /prisma/ migrations /) 20260703000028_init — migración inicial (crea User presumiblemente, y enum Role). 20260714005334_add_organization — agrega tabla Organization , enum OrganizationStatus , y FK Organization.ownerId → User.id (ON DELETE RESTRICT ON UPDATE CASCADE). Estado actual del modelo de datos : solo hay 2 entidades ( User , Organization ). Todo lo relacionado a Eventos, Entradas/Tickets, Ventas, Scanners que se ve en el frontend ( mock data) no tiene todavía modelo Prisma ni tablas — es funcionalidad pendiente de persistir en base de datos real. 4. CONFIGURACIÓN backend / prisma.config.ts : define paths de schema /migraciones y datasource URL vía env (patrón nuevo de Prisma 6, reemplaza configuración por CLI flags ). frontend /vite.config.js : plugins React + Tailwind v4. . vscode / settings.json (raíz): configuración de editor (no inspeccionado en detalle, es de VSCode ). . claude / settings.local.json (raíz): configuración local de Claude Code (permisos, etc.) — no relevante para arquitectura de la app. Variables de entorno : no hay archivos . env .example ni en backend ni en frontend — solo los . env reales ( gitignored ) con: Backend : DATABASE_URL, CLERK_PUBLISHABLE_KEY, CLERK_SECRET_KEY Frontend : VITE_CLERK_PUBLISHABLE_KEY Se recomendaría crear . env .example en ambos para documentar sin exponer secretos. 5. ARCHIVOS SUELTOS EN LA RAÍZ localhost.session.sql : archivo vacío (0 líneas/0 bytes). Nombre sugiere que fue creado por alguna herramienta de administración de base de datos (ej. una extensión de VSCode tipo " SQLTools " o similar) al abrir una sesión/conexión llamada "localhost" contra Postgres , pero nunca se guardó contenido en él. No tiene impacto funcional; se podría borrar o agregar a . gitignore . package.json (raíz) : { " dependencies ": { " framer-motion ": "^12.42.2 " } } Es un package.json mínimo y suelto en la raíz del repo (no es un workspace real, no tiene name /scripts/ workspaces ), con node_modules propio también en la raíz. Aparenta ser un remanente de una prueba/instalación de framer-motion (librería de animaciones) que no está siendo importada actualmente en ningún archivo del frontend inspeccionado — posible resto de experimentación o dependencia pensada para agregarse al frontend pero instalada por error en la raíz. Vale la pena preguntarle al usuario si se puede eliminar o si framer-motion debe migrarse a frontend / package.json . . git / : repo git normal. 6. RESUMEN / OBSERVACIONES PARA EL ASISTENTE QUE CONTINÚE Backend muy incipiente : solo 2 modelos de datos ( User , Organization ) y 5 endpoints reales. Todo lo demás (eventos, entradas, ventas, scanners ) vive únicamente como mock data en el frontend ( OrganizerDataContext + data/organizerMockData.js), sin persistencia real. Auth : Clerk maneja login / signup /sesión en el frontend ; el backend sincroniza usuarios de Clerk a la tabla User local vía /api/ auth / sync , asignando rol DEVELOPER solo al email hardcodeado ezequiel.maureira@gmail.com, y CUSTOMER a todos los demás por defecto. El rol pasa a ORGANIZER automáticamente al crear una organización. Falta de autorización granular : no hay middleware de verificación de rol en las rutas backend (ej. /api/ users no exige ni siquiera estar autenticado); sería un punto a reforzar. Frontend : React 19 + Router v7 + Tailwind v4, sin librería de estado global externa, con Contexts propios. UI cuidada con dark theme consistente y kit de componentes propio. Roles del sidebar se infieren de la URL ( roleFromPath ), no del rol real sincronizado del backend — desalineación potencial entre UI mostrada y permisos reales del usuario. Pendientes evidentes : modelos Prisma para Event /Ticket/Sale/Scanner; endpoints CRUD correspondientes; conectar OrganizerDataContext a la API real en lugar de mock data; agregar . env .example ; limpiar package.json / localhost.session.sql sueltos en la raíz; agregar middleware de autorización por rol en el backend . Ya tengo el informe completo. Te lo dejo abajo en formato markdown , listo para copiar y pegar en ChatGPT . Informe Técnico — Proyecto " PaseCultural " Stack general: Monorepo con backend / (Node.js + Express 5 + Prisma 6 + PostgreSQL + Clerk) y frontend / ( React 19 + Vite 8 + React Router 7 + Tailwind 4 + Clerk). 1. Backend ( backend /) Entry point : src /server.js → src /app.js (Express, CORS, JSON, clerkMiddleware ( )). Escucha en PORT (default 3000). Patrón: Router → Controller → Service → Prisma (sin capa " models " separada). Estructura: controllers/, routes/, services/, config/prisma.js (singleton de PrismaClient ). Endpoints actuales Método Ruta Descripción GET /api/ health Healthcheck POST /api/ auth / sync Sincroniza usuario de Clerk con tabla User local GET /api/ users Lista todos los usuarios ( ⚠️ sin protección de auth ) GET /api/ users / count Cuenta usuarios GET /api/ organizations /me Organización del usuario autenticado POST /api/ organizations Crea organización (asciende rol CUSTOMER→ORGANIZER) Auth: Clerk. No hay requireAuth ( ) como middleware de router — cada controller chequea getAuth ( req ). userId manualmente. /api/ users no verifica autenticación en absoluto. Rol developer se asigna solo al email hardcodeado ezequiel.maureira@gmail.com en auth.service.js. 2. Base de datos (Prisma/PostgreSQL) Solo 2 modelos: User : id, clerkId , email, firstName , lastName , imageUrl , role (DEVELOPER|ORGANIZER|SCANNER|CUSTOMER), timestamps . 1-a-muchos con Organization . Organization : id, name , description , logo, phone , email, website , cuit , status (PENDING|APPROVED|REJECTED|SUSPENDED), approvedAt / By , ownerId ( FK→User ). 2 migraciones: init y add_organization . No existen modelos para Eventos, Entradas, Ventas ni Scanners todavía — solo viven como mock data en el frontend . 3. Frontend ( frontend /) Routing : React Router v7. PublicShell (Home, landing organizadores, login / signup ) + rutas protegidas con RequireAuth (Clerk SignedIn / SignedOut ). Dashboards por rol: / developer , /organizador (+ subrutas : eventos, entradas, ventas, scanners , configuración), /scanner, /usuario. Estado: sin librería externa — Context API propio: AuthContext : sincroniza sesión Clerk con backend vía POST /api/ auth / sync . OrganizerDataContext : CRUD 100% in-memory / mock , no persiste a la API (datos en data/organizerMockData.js). API layer : lib /api.js — wrapper simple de fetch con VITE_API_URL (default localhost:3000). UI: kit propio (Button, Card, Modal, FormField , etc.) con Tailwind 4, tema oscuro. Nota: el Sidebar infiere el rol activo desde la URL ( roleFromPath ), no desde el rol real del usuario — no restringe navegación por permisos reales. 4. Configuración / entorno Backend .env : DATABASE_URL, CLERK_PUBLISHABLE_KEY, CLERK_SECRET_KEY. Frontend . env : VITE_CLERK_PUBLISHABLE_KEY. No hay . env .example en ningún lado. prisma.config.ts usa el patrón nuevo de Prisma 6 ( config por archivo en vez de flags CLI). 5. Archivos sueltos en la raíz (a revisar) localhost.session.sql : archivo vacío, probablemente resto de una herramienta de DB ( SQLTools o similar). Se puede borrar. package.json raíz con solo framer-motion como dependencia, sin name /scripts. No se usa en ningún archivo actual del frontend — parece instalación suelta/experimental, con su propio node_modules en la raíz. 6. Estado del proyecto y pendientes clave Backend muy incipiente: solo User y Organization persistidos; todo lo de Eventos/Entradas/Ventas/ Scanners es mock en frontend , sin conexión real. Falta autorización por rol en el backend (middleware de verificación de rol/ auth en rutas). Falta conectar OrganizerDataContext a endpoints reales. Faltan . env .example en ambos proyectos. Limpieza recomendada: localhost.session.sql y el package.json / node_modules sueltos en la raíz. 